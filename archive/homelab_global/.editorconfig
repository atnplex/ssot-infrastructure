root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
# Allowing trailing spaces in Markdown tables etc. can be useful, but
# for now keep it strict; relax later if it causes noise.
trim_trailing_whitespace = true
